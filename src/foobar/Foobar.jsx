import { StyledFoobar } from './Foobar.styles';

const Foobar = () => {
  const color = 'peru';
  return <StyledFoobar color={color}>Here we go, initial commit!</StyledFoobar>;
};

export default Foobar;
